{
  "name": "@apolopanel/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/agent.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@apolopanel/shared": "workspace:*",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "tsx": "^4.19.1"
  }
}

